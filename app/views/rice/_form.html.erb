<%= form_with(model: rice) do |form| %>
  <% if rice.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rice.errors.count, "error") %> prohibited this rice from being saved:</h2>

      <ul>
        <% rice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :kg, style: "display: block" %>
    <%= form.number_field :kg %>
  </div>

  <div>
    <%= form.label :brand, style: "display: block" %>
    <%= form.text_field :brand %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
